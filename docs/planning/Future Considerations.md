---
status: active
tier: working
last-reviewed: YYYY-MM-DD
read-when: "at phase open (check for fired triggers) and phase close (prune shipped)"
---

# Future Considerations

<!-- PARSED by lib/system.ts (getFutureItems) -> /system/future. Changing this section's SHAPE
     (the '## FCn. Title' sections with **Trigger:** / **Context:** / **Effort:** / **Refs:** / **Added:** fields) breaks that page silently - the /system drift banner will name it.
     Check /system after editing. Spec: docs/implementation/system-surface.md -->

Known directions waiting for a trigger — not questions to answer (those are Open Questions), not concrete fixes (those are the punch list), but known-direction items waiting to land. One of three planning trackers — see `CONTRIBUTING.md` → "The Planning Trackers." Items **graduate out** — to the punch list, a phase board, or feature scope — when their trigger fires.

---

_(No future considerations yet. Add them as `## FC1. Title` with **Trigger:** / **Context:** / **Effort:** / **Refs:** / **Added:** fields. Delete this line when you add the first.)_
